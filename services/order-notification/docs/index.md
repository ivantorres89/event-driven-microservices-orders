# order-notification

SignalR hub: pushes real-time order status updates. Uses Redis backplane for scale-out.

## Local endpoints
- Base URL: `https://localhost:5007`
- SignalR Hub: `https://localhost:5007/hubs/order-status`

## Runbook
- See `runbook.md`.
