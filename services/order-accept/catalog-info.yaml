apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: order-accept
  description: HTTP entry point for the order workflow (products API + order intake).
  tags:
    - contoso-orders
    - azure
    - aks
    - dotnet
  annotations:
    backstage.io/techdocs-ref: dir:.
    backstage.io/kubernetes-id: order-accept
spec:
  type: service
  lifecycle: production
  owner: group:platform
  system: contoso-orders
  dependsOn:
    - resource:default/contoso-orders-redis
    - resource:default/contoso-orders-sql
    - resource:default/contoso-orders-servicebus
