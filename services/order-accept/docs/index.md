# order-accept

HTTP entry point: products API + order intake. Initializes workflow state and publishes OrderAccepted.

## Local endpoints
- Base URL: `http://localhost:8081`

## Runbook
- See `runbook.md`.
